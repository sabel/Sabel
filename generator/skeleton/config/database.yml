production:
  default:
    driver: mysql
    schema: default
    host: localhost
    database: default
    user: root
    password:

test:
  default:
    driver: mysql
    schema: default
    host: localhost
    database: default
    user: root
    password:

development:
  default:
    driver: mysql
    schema: default
    host: localhost
    database: default
    user: root
    password:
