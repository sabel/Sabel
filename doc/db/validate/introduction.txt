データベースへの登録・更新など、入力データの検証が必要になる時があるでしょう。
sabel.dbでは、データベースのテーブル(スキーマ)情報を利用し、簡単に入力値のチェックを行うことができます。
入力値チェックの面倒なコードや、面倒な設定ファイル・パラメータから解放されます。
テーブル作成時にnameカラムをNOT NULLで宣言したならば、そのnameカラムはアプリケーションに関係なくNOT NULLなはずです。

また、テーブル情報からは判断できないようなルールは、カスタムバリデータを作成することで対応可能です。

